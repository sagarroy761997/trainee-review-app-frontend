import { makeStyles } from '@mui/styles';

export default makeStyles({
  root: {
    height: '100vh',
    display: 'flex',
    flexDirection: 'column',
    gap: '5%',
    alignItems: 'center',
    justifyContent: 'center',
  },

});
